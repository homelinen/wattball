<h1>New athlete</h1>

<%= form_for(@athlete) do |f| %>
  <%= render 'form', :locals => { :f => f } %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', athletes_path %>
