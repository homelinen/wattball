<%= form_for(@competition) do |f| %>
  <% if @competition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@competition.errors.count, "error") %> prohibited this competition from being saved:</h2>

      <ul>
      <% @competition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ticket_limit %><br />
    <%= f.text_field :ticket_limit %>
  </div>
  <div class="field">
    <%= f.label :adult_price %><br />
    <%= f.text_field :adult_price %>
  </div>
  <div class="field">
    <%= f.label :concession_price %><br />
    <%= f.text_field :concession_price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
