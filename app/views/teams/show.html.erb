<p id="notice"><%= notice %></p>

<p>
  <b>User:</b>
  <%= @team.User.name %>
</p>

<p>
  <b>Teamname:</b>
  <%= @team.teamName %>
</p>

<p>
  <b>Badge:</b>
  <%= image_tag @team.badge.url(:thumb) %>
</p>

<p>
  <b>Players</b>
  <ul class="playerList">
    <% WattballPlayer.where(:team_id => @team.id).each do |player| %>
        <li>
          <%= link_to player.user.name, athlete_path(player) %>
        </li>
    <% end %>
  </ul>
</p>

    
<p>
  <b>Matches:</b>
  <% list = "" %>
  <% WattballMatch.where("team1_id = :team OR team2_id = :team", :team => Team.first.id).each do |match|
    list += content_tag :li, link_to("#{match.team1.teamName} vs #{match.team2.teamName}", match)
  end %>
  <%= content_tag :ul, list.html_safe %>
  </ul>
</p>

<p>
  <% if @team.tournament %>
    Registered for
    <%= link_to @team.tournament.name, @team.tournament %>.
  <% else %>
    Not Registered for a Tournament (Can't happen).
  <% end %>

</p>


<%= link_to 'Edit', edit_team_path(@team) %> |
<%= link_to 'Back', teams_path %>
